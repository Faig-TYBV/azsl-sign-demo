# Vercel serverless function deps — auth + web layer ONLY.
# Deliberately excludes torch / mediapipe / opencv (the recognition stack):
# they blow past Vercel's 250 MB function limit and need a long-lived process.
fastapi>=0.115.0
sqlalchemy>=2.0.0
psycopg[binary]>=3.1
argon2-cffi>=23.1.0
itsdangerous>=2.1.0
python-dotenv>=1.0.0
email-validator>=2.0.0
