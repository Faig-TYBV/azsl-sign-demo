# Web layer + PostgreSQL auth. This is what Vercel installs for the serverless
# function (api/index.py). Keep it lean.
#
# The sign-language recognition stack (torch, mediapipe, opencv, ...) lives in
# requirements-recognition.txt — it cannot run on Vercel and would blow the
# 250 MB function limit. To run the full recognition backend
# (src/web_demo/backend.py) locally or in a container:
#     pip install -r requirements.txt -r requirements-recognition.txt
fastapi>=0.115.0
sqlalchemy>=2.0.0
psycopg[binary]>=3.1
argon2-cffi>=23.1.0
itsdangerous>=2.1.0
python-dotenv>=1.0.0
email-validator>=2.0.0
